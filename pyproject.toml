[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oracle-engine"
version = "1.0.0"
description = "Fortune Telling API - Tarot, Thai Astrology, Western Astrology"
requires-python = ">=3.11"
